# OpenAI Quickstart in TypeScript
Collection of OpenAI tutorials and custom code in TypeScript

```sh
# Setup
npm install --global tsx
brew install dotenvx
touch .env
npm install
```

```sh
# Running
dotenvx run -- tsx ai
dotenvx run -- tsx speech-to-text
```
